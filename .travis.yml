language: python
python:
  - '3.8'

# safelist
branches:
  only:
    - staging

before_script:
  - pip install -r requirements.txt

services:
  - postgresql

env: DJANGO_SETTINGS_MODULE=project8.project8.settings.travis

script:
  - ./project/8manage.py test